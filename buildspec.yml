version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies..."

  build:
    commands:
      - echo "Build started..."
      - ls -la
      - echo "Build completed successfully."

artifacts:
  files:
    - '**/*'
